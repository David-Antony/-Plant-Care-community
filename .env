# Local MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
DB_NAME=plant_care_db
COLLECTION_NAME=submissions

# Instructions:
# 1. If using MongoDB locally, keep the above as is.
# 2. If using MongoDB Atlas (Cloud), replace MONGODB_URI with your Atlas connection string.
#    Example: MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/test
